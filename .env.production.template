# Production Environment Variables Template
# Copy this file to .env.production and fill in your actual values

# Database Configuration
DATABASE_URL="postgresql://postgres:[YOUR_PASSWORD]@[SUPABASE_HOST]:5432/postgres"

# NextAuth Configuration
NEXTAUTH_SECRET="[GENERATE_32_CHARACTER_SECRET]"
NEXTAUTH_URL="https://[YOUR_VERCEL_DOMAIN].vercel.app"

# Optional: Supabase Configuration (if using additional features)
SUPABASE_URL="https://[YOUR_PROJECT_ID].supabase.co"
SUPABASE_ANON_KEY="[YOUR_SUPABASE_ANON_KEY]"

# Optional: Email Configuration (for notifications)
SMTP_HOST="your-smtp-host"
SMTP_PORT="587"
SMTP_USER="your-email@domain.com"
SMTP_PASS="your-email-password"

# Generate NextAuth secret with:
# openssl rand -base64 32

# Notes:
# 1. Never commit this file to version control
# 2. For Vercel deployment, add these variables in the Vercel dashboard
# 3. For Supabase, get the DATABASE_URL from your project settings
# 4. Make sure NEXTAUTH_URL matches your production domain exactly
